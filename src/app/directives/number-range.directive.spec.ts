import { NumberRangeDirective } from './number-range.directive';

describe('NumberRangeDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberRangeDirective();
    expect(directive).toBeTruthy();
  });
});
